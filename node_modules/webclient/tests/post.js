'use strict';
var WebClient = require('../');

WebClient.post({
    url: 'http://127.0.0.1:8000/123',
    qs: {
        'query': 'google',
        'submit': 'Submit'
    },
    form: {
        'queryPost': 'google',
        'submitPost': 'Submit'
    },
    headers: {
        // 'Referer': 'pornhub.com',
        'User-Agent': 'pornhub tester'
    },
    referer: 'test.net'
}).then(function(result) {
    let response = result[0], body = result[1];
    response.body = body.length;
    // console.log(response);

    console.log(body);
}).catch(function(err) {
    console.log(err);
});
